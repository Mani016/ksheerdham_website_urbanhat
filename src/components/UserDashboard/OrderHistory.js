import React from 'react';
const OrderHistory = () => {
    React.useEffect(()=>{
        // billingHistory
    },[])
    return (
        <React.Fragment>

        </React.Fragment>
    )
}
export default OrderHistory;